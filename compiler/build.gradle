group 'me.mikeholler.mannotate'
version '0.0.1'

apply plugin: 'java'

sourceCompatibility = 1.5

repositories {
    mavenCentral()
}

dependencies {
    compile project(':annotations')
    compile 'jdom:jdom:1.1'
    testCompile group: 'junit', name: 'junit', version: '4.11'
}
